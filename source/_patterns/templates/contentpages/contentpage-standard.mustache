{{> organisms-top-bar }}
<div class="container">
    <div class="row">
        <div class="col-md-8">

            {{> atoms-headline-main }}
            {{> organisms-content-container }}

            {{> molecules-headline-icon-with-link(iconHeadlineLabel: "Überschrift mit Icon und Link") }}
            {{> molecules-headline-icon }}

            {{> organisms-link-block-intern }}

        </div>
        <div class="col-md-4">
            Right column
        </div>
    </div>
</div>